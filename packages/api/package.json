{
  "name": "@cipher-atlas/api",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "check-types": "tsc -b",
    "test": "vitest run --maxWorkers=1 --exclude=dist/**"
  },
  "dependencies": {
    "@cipher-atlas/auth": "workspace:*",
    "@cipher-atlas/db": "workspace:*",
    "@cipher-atlas/env": "workspace:*",
    "@cipher-atlas/scan-domain": "workspace:*",
    "@trpc/client": "catalog:",
    "@trpc/server": "catalog:",
    "@types/pg": "^8.16.0",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cipher-atlas/config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "^4.1.9"
  }
}
